# Freshy CLI

> Reduce food waste with intelligent expiration date tracking, entirely from the command line.

**Freshy Solutions** — Java 17, zero external dependencies.

---

## Table of contents

1. [Prerequisites](#prerequisites)
2. [Project structure](#project-structure)
3. [Build and run](#build-and-run)
4. [Default accounts](#default-accounts)
5. [Features](#features)
6. [Roles and permissions](#roles-and-permissions)
7. [CSV data format](#csv-data-format)
8. [Automatic backup](#automatic-backup)
9. [Technical architecture](#technical-architecture)

---

## Prerequisites

- **Java 17** or higher
- No Maven/Gradle dependency — compile directly with `javac`

---

## Project structure

```
freshy-cli/
├── src/main/java/freshy/
│   ├── Main.java                  # Entry point
│   ├── cli/
│   │   └── MenuManager.java       # All menus and CLI interactions
│   ├── model/
│   │   └── FoodItem.java          # Perishable food item model
│   ├── users/
│   │   ├── User.java              # Abstract base class
│   │   ├── NormalUser.java
│   │   ├── PowerUser.java
│   │   └── AdminUser.java
│   ├── service/
│   │   ├── FoodService.java       # Food item business logic
│   │   ├── UserService.java       # User business logic
│   │   ├── MetricsService.java    # Statistics dashboard
│   │   └── ExportService.java     # CSV export + backups
│   └── storage/
│       ├── FoodRepository.java    # food_items.csv persistence
│       └── UserRepository.java    # users.csv persistence
├── data/
│   ├── food_items.csv             # Main inventory
│   ├── users.csv                  # User accounts (created on first launch)
│   └── backups/                   # Automatic backups
├── exports/                       # Exports generated by Power/Admin users
└── README.md
```

---

## Build and run

### Compile

```bash
# From the project root
javac -d out --release 17 $(find src -name "*.java")
```

### Run

```bash
java -cp out freshy.Main
```

> The program must be launched from the project root (`freshy-cli/`) so that relative paths to `data/` and `exports/` resolve correctly.

---

## Default accounts

Created automatically on first launch if `users.csv` is empty or missing:

| Username    | Password    | Role   |
|-------------|-------------|--------|
| `admin`     | `admin123`  | ADMIN  |
| `poweruser` | `power123`  | POWER  |
| `user`      | `user123`   | NORMAL |

> Passwords are stored as SHA-256 hashes in `users.csv`.

---

## Features

### Authentication
- Maximum 3 attempts before automatic shutdown
- Password is SHA-256 hashed before comparison

### Food CRUD
| Option | Action |
|--------|--------|
| 1 | View all items (formatted table with EXPIRED / SOON / OK status) |
| 2 | Add an item (name, category, quantity, unit, expiration date) |
| 3 | Edit an item (leave blank = keep current value) |
| 4 | Delete an item (confirmation required) |
| 5 | View items expiring within ≤ 3 days |

### Metrics (Power + Admin)
- Total items in stock
- Number of items expiring within 3 days
- Number of already expired items
- Most represented category
- Item with the nearest expiration date

### CSV export (Power + Admin)
Generates a timestamped file in `exports/`:
```
exports/freshy_export_2026-03-20_14-30.csv
```

### User management (Admin only)
- List all users (passwords hidden)
- Create a user (choose role)
- Delete a user (except yourself)
- Change a user's role (except yourself)

### CSV import (Admin only)
- Path to an external CSV file in `food_items.csv` format
- Duplicate-free merge (based on ID)
- Summary: X imported, Y skipped

---

## Roles and permissions

| Feature                  | Normal | Power | Admin |
|--------------------------|:------:|:-----:|:-----:|
| View items               | ✅     | ✅    | ✅    |
| Add an item              | ✅     | ✅    | ✅    |
| Edit an item             | ✅     | ✅    | ✅    |
| Delete an item           | ✅     | ✅    | ✅    |
| Items expiring soon      | ✅     | ✅    | ✅    |
| Metrics dashboard        | ❌     | ✅    | ✅    |
| Export inventory         | ❌     | ✅    | ✅    |
| Manage users             | ❌     | ❌    | ✅    |
| Import CSV               | ❌     | ❌    | ✅    |

---

## CSV data format

### `data/food_items.csv`
```
id,name,category,quantity,unit,expirationDate,addedBy,addedDate
```

| Field          | Type       | Possible values                 |
|----------------|------------|---------------------------------|
| id             | UUID       | Auto-generated                  |
| name           | String     | Free text                       |
| category       | Enum       | Dairy, Meat, Vegetable, Fruit, Other |
| quantity       | int        | > 0                             |
| unit           | Enum       | kg, g, L, pieces                |
| expirationDate | LocalDate  | YYYY-MM-DD                      |
| addedBy        | String     | Username of the author          |
| addedDate      | LocalDate  | YYYY-MM-DD (auto)               |

### `data/users.csv`
```
id,username,password,role,createdAt
```

| Field     | Type      | Possible values             |
|-----------|-----------|-----------------------------|
| id        | UUID      | Auto-generated              |
| username  | String    | Unique, case-insensitive    |
| password  | String    | SHA-256 hash (64 hex chars) |
| role      | Enum      | NORMAL, POWER, ADMIN        |
| createdAt | LocalDate | YYYY-MM-DD                  |

---

## Automatic backup

On every startup, `food_items.csv` is copied to `data/backups/`:
```
data/backups/food_items_backup_2026-03-20.csv
```

Only the **last 5 backups** are kept (older ones are deleted automatically).

---

## Technical architecture

```
Main
 └── ExportService      (backup on startup)
 └── FoodRepository     (loads food_items.csv)
 └── UserRepository     (loads users.csv + seeds if empty)
 └── FoodService        (delegates to FoodRepository)
 └── UserService        (delegates to UserRepository)
 └── MetricsService     (computes stats from FoodService)
 └── MenuManager        (CLI UI, role-gated action dispatch)
```

### Technical choices
- **Zero external dependencies**: `java.time`, `java.io/nio`, `java.security`, `java.util`
- **Immediate persistence**: every modification (add/update/delete) flushes the CSV to disk
- **Java inheritance**: `NormalUser → PowerUser → AdminUser` with overridden `canXxx()` methods
- **Robustness**: all invalid inputs are caught without crashing (`NumberFormatException`, `DateTimeParseException`, `IllegalArgumentException`)

---

*Freshy Solutions © 2026*
